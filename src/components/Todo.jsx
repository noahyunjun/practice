import React from "react";
import PropTypes from "prop-types";

const Todo = (props) => {
  return (
    <div>
      <h1 className="underline">test</h1>
    </div>
  );
};

Todo.propTypes = {};

export default Todo;
